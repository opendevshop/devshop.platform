---
apps:
  children:
    drupal:
      vars:
        devshop_app_command_build: composer install
        devshop_app_command_deploy: bin/drush updb -y
        devshop_app_command_install: bin/drush site-install

