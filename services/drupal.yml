---
apps:
  children:
    drupal:
      vars:
        devshop_app_command_build: composer install
        devshop_app_command_install: vendor/bin/drush site-install -y
        devshop_app_command_deploy: vendor/bin/drush updb -y
        devshop_app_command_test: vendor/bin/drush status

